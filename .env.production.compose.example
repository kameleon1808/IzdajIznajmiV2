# Copy to .env.production.compose and adjust values before first production run.
# Usage: docker compose -p izdaji_prod --env-file .env.production.compose -f docker-compose.production.yml up -d --build

APP_URL=http://localhost
FRONTEND_URL=http://localhost
FRONTEND_URLS=http://localhost
SANCTUM_STATEFUL_DOMAINS=localhost,*.trycloudflare.com
SESSION_DOMAIN=
SESSION_SECURE_COOKIE=false
SESSION_SAME_SITE=lax
LOG_LEVEL=warning

POSTGRES_DB=izdaji_iznajmi
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres

REVERB_APP_ID=local
REVERB_APP_KEY=local
REVERB_APP_SECRET=local
REVERB_ALLOWED_ORIGINS=localhost,*.trycloudflare.com

MEILISEARCH_KEY=change-me
VITE_SEARCH_V2=true
VITE_ALLOWED_HOSTS=localhost,127.0.0.1,.trycloudflare.com
